#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    string s;
    cin >> x;
    cin.ignore();    // to ignore the enter after the inter input
    getline(cin, s); // taking inputs of strings which has spaces
    cout << x << endl;
    cout << s << endl;

    return 0;
}